TODO
====

General:

--info
--out FILE
--clean (deletes unreferenced coords adjusting indices)
FILE [FILE...]
GUI frontend?
separate object ("o") support
don't run analyzing pass if not needed

Operations on vertices:

--translate[xyz]
--rotate[xyz]
--scale[xyz]
--scale-absolute[xyz]

Normals:

--auto-normals [THRESHOLD-ANGLE]
--smooth-normals
--flat-normals
--normalize-normals

Faces:

--triangulate

Groups:

--delete-material MTL
--delete-group GRP
--delete-object OBJ

